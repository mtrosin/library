{% extends 'common/internal.html.twig' %}

{% block title %}Livros - Modificar{% endblock %}
{% block bclass %}sb-nav-fixed{% endblock %}

{% block content %}
    <h1 class="mt-4">Livros</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item">Livros</li>
        <li class="breadcrumb-item active">Modificar</li>
    </ol>
    <div class="card mb-4">
        <div class="card-header">
            <i class="fas fa-table me-1"></i>
            Modificar Livro
        </div>
        <div class="card-body">
            {{ include('book/_form.html.twig', {'button_label': 'Atualizar'}) }}

            {{ include('book/_delete_form.html.twig') }}

            <a href="{{ path('book_index') }}">Voltar para a listagem</a>
        </div>
    </div>
{% endblock %}
